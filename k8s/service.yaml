apiVersion: v1
kind: Service
metadata:
  name: my-app
  namespace: my-app
  labels:
    app: my-app
spec:
  selector:
    app: my-app     # version selector 없음 → stable + canary 둘 다 트래픽 받음
  ports:
  - name: http
    port: 80
    targetPort: 5000